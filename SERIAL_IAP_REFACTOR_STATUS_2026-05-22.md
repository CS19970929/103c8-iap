# 串口 IAP 重构实施记录

## 保持不变的外部协议

串口升级仍使用原有 RS485/Modbus 风格命令，不改变上位机入口：

| 地址 | 命令 | 作用 |
| --- | --- | --- |
| `0xFFFD` | `0x10` | 连接/初始化升级会话 |
| `0xFFFE` | `0x10` | 写入一个 App 数据块 |
| `0xFFFF` | `0x10` | 完成升级并请求复位 |

旧数据块兼容规则保留：当 byte_count 为 `0` 时，数量字段按“数据字节数”解释，数据从 `u16Buffer[7]` 开始。

## 新的内部逻辑

- `Sci.c` 不再直接擦写 App Flash，只负责解析串口帧和生成 ACK/NACK。
- `iap_upgrade.c` 统一管理串口升级状态、写入地址、块计数、超时和错误码。
- `0xFFFD` 会重新初始化会话，并把升级标志保持为 `FLASH_TO_IAP_VALUE`。
- `0xFFFE` 只允许写入 `0x08004800` 到 `0x0801F800`，单块最大 1024B。
- 每块写入前擦除覆盖页，写入后逐半字回读校验。
- `0xFFFF` 必须在至少收到一块数据后才接受，并校验 App MSP/ResetHandler 后才写 `FLASH_TO_APP_VALUE`。
- 升级超时后保持 IAP 标志，不跳半个 App。

## 风险收敛

- 移除旧逻辑中 `while (FLASH_ErasePage(...) != FLASH_COMPLETE)` 的无限等待。
- 禁止 `0xFFFF` 在空镜像或坏向量时写 App 有效标志。
- 禁止块长度超过旧协议约定的 1024B，避免串口缓冲区越界后仍写 Flash。
